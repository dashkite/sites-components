main
  .block(class="form")
    form
      section
        .field
          label
            span Name
            input(
              name = "name"          
              type = "text"
              required = "true"
              value = name
            )
      footer
        nav
          a(name="cancel" href="#") Reset
          button.highlight Save