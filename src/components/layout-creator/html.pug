main
  .block(class="form")
    form
      section
        .field
          label
            span Name
            input(
              name = "name"          
              type = "text"
              required = "true"
            )
      footer
        nav
          a(name="cancel" href="#") Cancel
          button.highlight Create